-- create table if not exists users (
--   id serial primary key,
--   email text not null,
--   name text not null
-- );

-- insert into users (email, name) values ('example@email.com', 'test user');